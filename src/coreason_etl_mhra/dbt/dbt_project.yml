name: 'coreason_etl_mhra'
version: '1.0.0'
config-version: 2

profile: 'coreason_etl_mhra'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  coreason_etl_mhra:
    +materialized: view
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
